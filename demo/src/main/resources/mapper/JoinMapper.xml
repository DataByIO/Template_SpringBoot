<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.shop.backend.SecurityTest.Repository">
    <select id="memberInfo" parameterType="MemberEntity" resultType="HashMap">
        SELECT *
        FROM memberslist
        WHERE 1=1
        AND email = #{email}
        AND password = #{password}
    </select>

    <insert id="insertMember" parameterType="MemberEntity" resultType="HashMap">
        insert into member
        (
            id,
            username,
            password,
            is_admin,
            phone_number,
            gender,
            address,
            user_status,
            birth_date,
            profile_image,
            points,
            last_login_at,
            created_at,
            updated_at
        )
        values (
                   #{id},
                   '테스트',
                   #{password},
                   'N',
                   '010-1111-1111',
                   'M',
                   '서울특별시 강북구',
                   'Y',
                   '1997-11-07',
                   '',
                   0,
                   SYSDATE(),
                   SYSDATE(),
                   SYSDATE()
               );
    </insert>

</mapper>